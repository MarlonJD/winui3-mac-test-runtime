#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

exec dotnet "$ROOT_DIR/src/WinUI3.MacRunner/bin/Debug/net10.0/WinUI3.MacRunner.dll" "$@"
